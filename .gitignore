cat > .gitignore << 'EOF'
# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so

# Virtual envs
venv/
.venv/
env/
ENV/
.sri/  # ignore if you accidentally created one
# Pip & build artifacts
*.log
pip-wheel-metadata/
site/
dist/
build/
*.egg-info/
.eggs/

# --- Django ---
# Local DB (keep out of git). If you truly deploy SQLite in prod, remove this line.
db.sqlite3
*.sqlite3

# Collected static and uploaded media (keep sources in /static/, but ignore collectstatic output)
staticfiles/
static_root/
media/
public/media/

# If you use whitenoise: ignore collectstatic output path if different
static_cdn/
media_cdn/

# Django cache & test artifacts
*.pot
*.mo
.coverage
htmlcov/
.pytest_cache/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
Thumbs.db

# Env/config secrets
.env
.env.*
*.pem
*.key

# Node (if you ever have frontend bits)
node_modules/
npm-debug.log*
yarn.lock
package-lock.json
EOF
